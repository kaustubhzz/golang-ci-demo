name: CI Pipeline — Build, Lint, Test, Secure, Add Docker and Deploy

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.24'

      - name: Cache Go modules
        uses: actions/cache@v3
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Download dependencies
        run: go mod download

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest

      - name: Run Tests with Coverage
        run: |
          go test ./... -v -covermode=atomic -coverprofile=coverage.out
          go tool cover -func=coverage.out

      - name: Upload coverage and metrics to QLTY
        uses: qltyhq/qlty-action@v1
        with:
          token: ${{ secrets.QLTY_TOKEN }}
          coverage-file: coverage.out
          fail-ci-on-error: true

      # Upload coverage file to GitHub as artifact for manual viewing
      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.out

      - name: Install and Run GoSec
        run: |
          echo "Installing GoSec..."
          go install github.com/securego/gosec/v2/cmd/gosec@latest
          echo "Running GoSec..."
          $HOME/go/bin/gosec ./...
        continue-on-error: true

      - name: Install and Run Govulncheck
        run: |
          echo "Installing Govulncheck..."
